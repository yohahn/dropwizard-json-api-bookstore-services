database:
  driverClass: com.mysql.jdbc.Driver
  user: bookstore
  password: bookstore
  url: jdbc:mysql://localhost:3306/bookstore
  properties:
    charSet: UTF-8
  validationQuery: "SELECT 1"

logging:
  level: INFO

  appenders:
    - type: console
