database:
  driverClass: com.mysql.jdbc.Driver
  user: bookstore
  password: bookstore
  url: jdbc:mysql://localhost:3306/bookstore
  properties:
    charSet: UTF-8
  validationQuery: "SELECT 1"

katharsis:
  host: http://localhost:8080
  searchPackage: name.yohahn.bookstore.domain

logging:
  level: INFO

  appenders:
    - type: console
